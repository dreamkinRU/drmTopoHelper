--- a/index.html
+++ b/index.html
@@ -1818,30 +1818,12 @@
 		a.innerHTML = h(e.isLocked?"lock":"lock_open"),n.appendChild(a),
 
 			this.renderBlockItems(t,e.items,e.bgColor),
-			L.state.blocks.forEach((e,t)=>{let n=document.getElementById(e.id);n&&(n.style.zIndex=t+2)})
-		t.classList.add("block-entering"),P.appendChild(t),
-			// Удаляем класс после проигрывания анимации
-			t.addEventListener("animationend",function(){
-				this.classList.remove("block-entering");
-				this.style.animation = ''
-			},{
-				once: true
-			}),
-			L.state.blocks.forEach((e,t)=>{let n=document.getElementById(e.id);n&&(n.style.zIndex=t+2)}),P.appendChild(t),
-			// Удаляем класс после проигрывания анимации для чистоты DOM
-			t.addEventListener("animationend",function(){
-				this.classList.remove("block-entering")
-			},{
-				once: true
-			}),
-			L.state.blocks.forEach((e,t)=>{let n=document.getElementById(e.id);n&&(n.style.zIndex=t+2)})
-
-		P.appendChild(t),L.state.blocks.forEach((e,t)=>{let n=document.getElementById(e.id);n&&(n.style.zIndex=t+2)})
+		this.renderBlockItems(t,e.items,e.bgColor);
+		t.classList.add("block-entering");
+		P.appendChild(t);
+		t.addEventListener("animationend",function(){
+			this.classList.remove("block-entering");
+			this.style.animation = '';
+		},{ once: true });
+		this.updateZIndices();
 	},
 	renderBlockItems(e,t,n){